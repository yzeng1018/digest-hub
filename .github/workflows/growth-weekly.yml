name: Growth Weekly Digest

on:
  schedule:
    - cron: "0 1 * * 6"   # 北京时间每周六 09:00（UTC 01:00）
  workflow_dispatch:

jobs:
  digest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: channels/growth-weekly/package-lock.json

      - name: 安装依赖
        run: npm install && npm install --prefix channels/growth-weekly

      - name: 运行 Growth Weekly digest
        env:
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          DIGEST_RECIPIENT:   ${{ secrets.DIGEST_RECIPIENT }}
          GROQ_API_KEY:       ${{ secrets.GROQ_API_KEY }}
          ZHIPU_API_KEY:      ${{ secrets.ZHIPU_API_KEY }}
        run: node channels/growth-weekly/main.js
