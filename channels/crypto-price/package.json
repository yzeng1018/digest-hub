{
  "name": "crypto-price",
  "version": "1.0.0",
  "description": "Daily crypto price radar — CoinGecko market data + AI trader insights",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env main.js",
    "dev": "node --env-file=.env main.js --no-email",
    "dry": "node --env-file=.env main.js --no-email --no-ai"
  },
  "dependencies": {
    "dotenv": "^17.4.1",
    "nodemailer": "^6.9.16",
    "openai": "^4.87.0"
  }
}
